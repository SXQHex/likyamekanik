---
description: Layout + Header + Language Switcher
---

Task: Build a professional site layout with global theme support.

Requirements:
1. Add dark/light theme system across the entire site.
2. Use next-themes package with ThemeProvider in layout.
3. Header must include:
- Logo text: Likya Mekanik
- Navigation links
- Theme toggle button (Light/Dark)
- Language switcher (TR/EN)

4. Tailwind must be configured with:
darkMode: "class"

5. All components must use semantic theme-friendly classes:
- bg-background
- text-foreground
- border-border

Expected output:
- Theme toggle works globally.
- Dark mode applies to all pages and components.